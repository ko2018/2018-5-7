package com.talent.front;
import java.util.Date;

import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.test.context.junit4.SpringRunner;

import com.talent.front.entity.StaticsPlatformBase;

@RunWith(SpringRunner.class)
@SpringBootTest
public class StaticsPlatformApplicationTests {
	
//	@Autowired
//	public StaticsPlatformBaseService staticsPlatformBaseService;
	
	@Test
	public void contextLoads() {
//		String str = staticsPlatformBaseService.toString();
//		System.out.println(str);
//		StaticsPlatformBase record = new StaticsPlatformBase ();
//		record.set_Id(""+2);
//		record.setStaticsId(""+3);
//		record.setAddTime(new Date());
//		staticsPlatformBaseService.insert(record);
//		staticsPlatformBaseService.insert(record);
	}
}
